<h1> Home Page </h1>

<% if logged_in? %>  
     <%= "Logged_in as: #{current_user.name}" %>
     <%= link_to "LogOut", logout_path, :method => "delete" %>
   <%else%>
      <%= link_to "SignUp", signup_path %> 
      <h4> OR </h4>
      <%= link_to "Login", login_path %>
      
  

  <% end%></br>
  
  <%= params %>
